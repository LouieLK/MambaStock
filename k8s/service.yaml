# k8s/service.yaml

apiVersion: v1
kind: Service
metadata:
  name: mambaquant-api-service
spec:
  type: NodePort
  
  selector:
    app: mambaquant
    
  ports:
    - protocol: TCP
      port: 8000        
      targetPort: 8000  
      nodePort: 30080   # (30000-32767)